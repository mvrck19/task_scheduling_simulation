#include <algorithm>
#include <functional>
#include <iostream>
#include <iterator>
#include <vector>

using namespace std;

enum state {
    READY,
    RUNNING,
    DONE
};

int main()
{

    state skata = READY;
    if(skata==READY) cout << "skata" << endl;


}